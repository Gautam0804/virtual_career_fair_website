const CandidateDashboard = () => {
    return (
      <section className="candidate-dashboard">
        <h2>Welcome, [Candidate Name]</h2>
        <p>Your applied jobs:</p>
        {/* Display applied jobs here */}
        <p>Your resume score:</p>
        {/* Display resume score */}
      </section>
    );
  };

  
export default CandidateDashboard;